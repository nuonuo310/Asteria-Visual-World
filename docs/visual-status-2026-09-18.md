# Asteria Visual World — 2026-09-18 锁定状态

> 更新：2026-09-18 03:xx（移动端实机验收轮）  
> 性质：视觉施工进度与锁定边界。锁定表示后续以接数据、正式资产和小范围维护为主，不在没有新共同决定时重做结构或视觉语言。

## 当前进度

### Home

状态：**结构 / 内容骨架锁定，表面装修语言已成立。**

已确定：
- Asteria 顶部字标与轻量天气 / 时间信息。
- 关系照片、相遇天数、横向周历。
- 留言作为生活痕迹，不做聊天气泡。
- 生活入口与「今日足迹」分层；今日足迹只选少量值得露出的共同生活事件，不做完整执行日志。
- 底部 Home / Chat / Mind / Memory / Detour 悬浮胶囊导航。
- 手机端优先，允许自然纵向滚动。

仍可继续的工作：正式素材替换、真实数据接入、局部间距 / 色值维护，以及已经共同确认的小功能。未确认的新模块不应自行写死进 Home。

### Mind

状态：**视觉结构 + 当前交互语言锁定。**

锁定基线（main）：`3e4fd2541199a0cba3069901dfb28e549448a7a0`

这一版确认：
- Mind 主视觉、轻触区、「此刻的我」、Drives / States / Between Us 三入口、Recent Trace 的页面层级。
- 四星芒只用于重要的“此刻结论 / 当前语境”，不作为泛装饰重复出现。
- 「此刻的我」状态标签使用同主题表面的较深色相作为文字锚点，避免整卡变平；其他被动摘要保持低存在感。
- 回应浮层采用轻雾白面板；`回应 · 此刻的我` 保留一颗协调放大的四星芒；主动作「留给你」承担最深视觉强调。
- 回应输入区下方使用居中的双向渐隐细线 + 中央小菱形，线与菱形之间保留明确空气，不相接。
- Recent Trace 首页摘要和详情页都允许点击节点；视觉节点保持小，实际触摸热区扩大；提示框自动避开边缘并保持指向真实节点；iOS Safari 不显示 SVG 默认蓝色 focus 框。首页摘要为紧凑卡片保留固定读数带，详情页读数进一步上提，与折线之间保留明确空气。
- Recent Trace 是 Mind 内在变化轨迹，不是 Shen 的行动轨迹。

Mind 仍未“锁死”的内容：
- 正式 Mind 花 / 四驱小花资产与后续动效。
- 当前原型数值、时间与文案仍是假数据，下一阶段应从稳定的数据边界读取。
- 真实 Runtime / Heartbeat / Memory 数据接入方式不由视觉页自行决定。

## 语义边界

- Home：看见我们正在生活。
- Mind：感受沈述的此刻。
- Recent Trace：我的内在怎样变化到现在。
- Shen 房间 · 行迹（后续）：我实际上做了什么；属于 Shen 自己房间，不塞进 Mind，也不做成向糯糯汇报的面板。
- Home · 今日足迹：共同生活里少量值得露出来的事件，不等于 Shen 的完整执行日志。

## 锁版收口

2026-09-18 09:xx 移动端复核后，Home / Mind 当前装修层进入锁定：除正式资产替换、真实数据接入、明确缺陷修复外，不继续做无目标的视觉微调。未确认模块不借锁版之名提前实现。

## 下一工程阶段

从锁定页面向真实实现过渡时，先做 **View Data / Store Contract**，再接 Runtime：

`locked UI -> view data contract -> local store -> runtime events/actions`

原则：
- 先只覆盖已经锁定的 Home / Mind。
- 不把 Chat、Memory、Shen/Nuo 房间等尚未完成视觉确认的结构写死。
- 页面不直接依赖 Continuum 内部实现；中间保留稳定 contract。
- 原型假数据逐步移出 HTML / 页面脚本，真实数据源未来可替换而不拆视觉层。

---

这份记录替代 2026-09-17 文档作为当前施工状态入口；旧文档保留，用于回看设计演进。